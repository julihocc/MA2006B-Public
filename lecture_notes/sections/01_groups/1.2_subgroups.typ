#import "../../utils.typ": solution, proof, definition, theorem, proposition, corollary, example, solution
== Subgroups

=== Theory
#definition(title: "Subgroup")[
  A subset $H$ of a group $G$ is called a *subgroup* if $H$ itself forms a group under the same operation defined on $G$. We denote this as $H lt.eq G$.
]

#proposition(title: "Subgroup Test")[
  A non-empty subset $H subset.eq G$ is a subgroup if and only if for all $a, b in H$, $a dot b^(-1) in H$.
]

#theorem(title: "Lagrange's Theorem")[
  If $G$ is a finite group and $H$ is a subgroup of $G$, then the order (number of elements) of $H$ divides the order of $G$. i.e., $|H|$ divides $|G|$.
  #proof[
    The left cosets of $H$ in $G$ form a partition of $G$. The map $h mapsto g h$ is a bijection between $H$ and $g H$, so every coset has the same size $|H|$. If there are $k$ distinct cosets, then $|G| = k |H|$. Thus $|H|$ divides $|G|$.
  ]
]

=== Computations
To find subgroups of $ZZ_6$ (under addition):
The order of $ZZ_6$ is 6. Possible orders for subgroups are divisors of 6: 1, 2, 3, 6.
- Order 1: ${0}$ (trivial subgroup)
- Order 2: ${0, 3}$ ($3+3=6 equiv 0$)
- Order 3: ${0, 2, 4}$ ($2+2=4, 4+2=6 equiv 0$)
- Order 6: $ZZ_6$ itself.

=== Applications
Subgroups play a crucial role in preventing "small subgroup attacks" in cryptographic protocols. In Diffie-Hellman, if the group order has small factors, an attacker can confine the shared secret to a small subgroup and exhaustively search for it. We often choose groups of prime order or groups where the order has a large prime factor to avoid this.

=== Exercises
+ List all subgroups of $ZZ_(12)$.
#solution[
  Divisors of 12 are 1, 2, 3, 4, 6, 12. Subgroups correspond to these orders.
  - Order 1: ${0}$
  - Order 2: ${0, 6}$
  - Order 3: ${0, 4, 8}$
  - Order 4: ${0, 3, 6, 9}$
  - Order 6: ${0, 2, 4, 6, 8, 10}$
  - Order 12: $ZZ_(12)$
]
+ Propose a reason why we might want to use a subgroup of prime order for cryptography.
#solution[
  Prime order subgroups have no proper non-trivial subgroups. This prevents small subgroup attacks where an attacker forces the computation into a smaller group to solve the discrete logarithm problem more easily.
]
+ Let $G = ZZ_7^*$ (under multiplication). Find the subgroup generated by the element $2$.
#solution[
  $ZZ_7^* = {1, 2, 3, 4, 5, 6}$.
  Powers of 2:
  $2^1 = 2$
  $2^2 = 4$
  $2^3 = 8 equiv 1$
  So the subgroup is ${1, 2, 4}$. Order is 3.
]
